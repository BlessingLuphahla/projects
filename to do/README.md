compreshensive to do app
please clone the repo or download the zip

create a venv and run the following CLI commands to install dependencies:
pip install flask
pip install flask_sqlalchem
pip install wtforms
